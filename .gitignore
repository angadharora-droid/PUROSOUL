# Dependencies & builds (also covered by nested .gitignore files — kept here as a safety net)
node_modules/
dist/

# Secrets — never commit environment files
.env
.env.local
.env.example

# Local artifacts (saved report attachments + import progress state)
backend/data/
registration-pdf-preview.pdf
Thumbs.db
.DS_Store
